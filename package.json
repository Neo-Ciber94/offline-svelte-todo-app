{
  "name": "root",
  "private": true,
  "packageManager": "bun@1.1.17",
  "workspaces": [
    "app",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo build",
    "format": "turbo format",
    "lint": "turbo lint",
    "check": "turbo check",
    "postinstall": "patch-package"
  },
  "trustedDependencies": [
    "patch-package"
  ],
  "devDependencies": {
    "patch-package": "^8.0.0",
    "turbo": "^2.0.11"
  }
}
